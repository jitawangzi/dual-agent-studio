# Technical Implementation Plan & Consensus Blueprint

> **Auto-generated by Dual-Agent Studio** (2026/8/28 23:05:53)
> **Initial Requirement**: "我想给当前项目进行一些优化"
> **Consensus State**: ✅ Consensus Reached

---

# Dual-Agent Studio 架构优化实施方案 (Round 3 修订案)

> **项目目标路径**: [D:/project/dual-agent-studio](file:///D:/project/dual-agent-studio)  
> **文档负责人**: `shuyongqiang` (Lead Software Architect & Developer)  
> **架构定位**: 通用零外部依赖双 Agent 协同研发驾驶舱与自愈引擎 (Node.js + PowerShell 7 + SSE + Vanilla Cockpit)

---

## 1. 针对 Reviewer 审查意见的逐项技术回应 (Direct Technical Response)

感谢 Reviewer 的精准审查与技术把关。审查意见指出的 3 项关键硬缺口（Windows 下 CLI 启动路由与 Aider 参数通道、Tracked Diff 内存防护与 64,000 字符限额、函数可测性解耦与讨论流中止）直接关乎系统的可用性与稳定性。现对该 3 项硬缺口给出彻底的技术闭环设计：

### 1.1 Windows CLI 进程启动路由、Provider 参数适配与线程安全 (P0 回应)
* **问题定位**：
  1. 当 `UseShellExecute = $false` 时，.NET `ProcessStartInfo` 无法直接把 `.cmd` / `.bat` / `.ps1` 作为 `FileName` 启动，会导致 `Win32Exception: The specified executable is not a valid application for this OS platform`；
  2. **Aider 不支持 Stdin 管道主指令输入**，且 Windows 命令行长参数易被截断；
  3. 多线程事件回调 `OutputDataReceived` 与 `ErrorDataReceived` 中，`StringBuilder` 缺乏并发锁保护，可能导致内部缓冲区损坏或字符丢失。
* **技术修正**：
  1. **扩展名自适应执行路由**：
     - 解析目标可执行文件绝对路径或扩展名；
     - 若为 `.cmd` / `.bat`：`FileName = "cmd.exe"`, `ArgumentList = @('/d', '/c', $cmdPath) + $arguments`；
     - 若为 `.ps1`：`FileName = "pwsh.exe"`, `ArgumentList = @('-NoProfile', '-File', $cmdPath) + $arguments`；
     - 若为 `.exe` 或原生二进制：直接作为 `FileName` 启动。
  2. **分 Provider 差异化入参适配**：
     - **GitHub Copilot CLI (`copilot`)**: 走 Stdin 管道流（`StandardInput.Write`），配合 `--allow-all`, `--session-id`, `--model`, `--reasoning-effort`；
     - **Aider (`aider`)**: 建立专属 UTF-8 临时指令文件，通过 `--message-file "<tempFile>"` 传入完整 Prompt，同时携带 `--yes-always`，并在 `finally` 块中确保清理临时文件，杜绝命令行截断；
     - **Claude Code (`claude`)**: 走 Stdin UTF-8 管道或 `--print` 模式，配合 `--model` 与环境变量 `MAX_THINKING_TOKENS`；
     - **Google Antigravity (`agy`)**: 走 `--print` 配合 UTF-8 Stdin 管道，注入 `--dangerously-skip-permissions`, `--model`, `--effort`；
     - **Cursor / Codex / Pi / Mock**: 遵循统一路由标准。
  3. **StringBuilder 线程安全互斥锁**：
     - 在 PowerShell 中引入同步锁对象 `$syncLock = [object]::new()`；
     - 在 `OutputDataReceived` 与 `ErrorDataReceived` 委托中统一使用 `[System.Threading.Monitor]::Enter($syncLock)` 与 `[System.Threading.Monitor]::Exit($syncLock)` 进行同步保护，确保线程安全。

### 1.2 Tracked Diff 按文件流式获取与 64,000 字符全局限额 (P0 回应)
* **问题定位**：直接调用 `git diff HEAD 2>&1 | Out-String` 会在一次性加载巨型文件变更时瞬间撑爆内存；且任务清单中存在 64KB 与 64,000 字符的混用。
* **技术修正**：
  1. **逐文件流式 Diff 提取（Tracked Modifications）**：
     - 先通过 `git diff --name-only -z HEAD` 获取发生变更的已跟踪相对文件路径列表；
     - 经过黑名单模式过滤（锁文件、node_modules、.git、dist、target、build、二进制文件、mailbox 等）；
     - 对每个文件单独调用 `git diff HEAD -- <filePath>`；
     - 若单文件大小超过 **256 KB** 或其 diff 输出超过 **256 KB**，直接写入摘要 `=== File: <path> (Size: X KB) - Exceeded 256KB limit, diff content skipped ===`，避免加载全量内容；
     - 累加到总 `StringBuilder`，一旦总字符数达到 **64,000 字符**，立即终止后续文件读取。
  2. **Untracked 文件对齐相同规则**：
     - 通过 `git status --porcelain -uall` 获取新增文件，应用相同的黑名单过滤、单文件 256 KB 上限与全局 64,000 字符累加预算。
  3. **指标描述全面统一**：
     - 所有文档、注释、单测与实现严格统一为 **64,000 字符**（约 16k tokens 上下文）。

### 1.3 内部函数解耦、测试套件落地与讨论流中止控制 (P0 回应)
* **问题定位**：
  1. `engine/orchestrator.ps1` 加载即运行主循环，内部函数无法单独引入单测；
  2. `server.js` 启动即 `listen`，直接 require 会导致端口抢占；
  3. 前台发起 `POST /api/discuss` 后，`/api/stop` 无法终止正在运行的讨论进程，亦无法重置 `isDiscussing` 互斥锁；
  4. 讨论单轮 180s 易被深度思考模型（如 R1 / o1 / Gemini 2.0 Flash Thinking）打断。
* **技术修正**：
  1. **提取核心模块库 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1)**：
     - 将 `Invoke-CliWithTimeout`, `Get-SafeWorkspaceDiff`, `Extract-TestFailureSummary`, `Format-CopilotReasoningEffort`, `Format-AgyReasoningEffort`, `Extract-JsonFromText`, `Write-MailboxState`, `Read-MailboxState`, `Invoke-DevTurn`, `Invoke-ReviewerTurn` 抽取到独立库文件；
     - [`engine/orchestrator.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator.ps1) 负责入口参数校验与执行主循环，开头通过 `. (Join-Path $PSScriptRoot "orchestrator-lib.ps1")` 加载库；
     - [`tests/orchestrator.tests.ps1`](file:///D:/project/dual-agent-studio/tests/orchestrator.tests.ps1) 可以点源直接引用 `orchestrator-lib.ps1`，直接对各个核心函数开展隔离单元测试。
  2. **Node.js 服务端导出支持与独立进程端到端测试**：
     - 改造 [`server.js`](file:///D:/project/dual-agent-studio/server.js)，仅在 `require.main === module` 时执行 `server.listen`；
     - 测试套件 [`tests/server.tests.js`](file:///D:/project/dual-agent-studio/tests/server.tests.js) 通过 `spawn('node', ['server.js'], { env: { PORT: randomPort } })` 启动独立进程，发起真实 HTTP 请求并进行接口断言，运行完毕后自动关闭进程。
  3. **`/api/stop` 全通道中止与状态重置**：
     - 维护全局 `activeDiscussionProcess` 与 `isDiscussing` 状态；
     - 当 `/api/stop` 触发时，同时检查并终止 `activeProcess` 与 `activeDiscussionProcess`（使用 Windows `taskkill.exe /F /T /PID` 强制杀进程树）；
     - 将 `isDiscussing` 重置为 `false`，清理临时 Prompt 文件，并向前端广播 `discussion_error` / `state_change` 解除 UI 禁用状态。
  4. **讨论单轮超时放宽至 600 秒**：
     - `executeDiscussionAgent` 单轮超时对齐为 **600 秒**（并支持通过请求体参数配置），充分满足复杂模型的深度思考需求。

---

## 2. 修订后的核心技术实现与代码设计规范 (Refined Technical Specifications)

### 2.1 Windows CLI 启动路由与分 Provider 执行器 (`Invoke-CliWithTimeout`)

在 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1) 中实现跨平台、多扩展名路由与线程安全的执行器：

```powershell
function Invoke-CliWithTimeout {
    param(
        [Parameter(Mandatory=$true)][string]$ExecutablePath,
        [Parameter(Mandatory=$false)][string[]]$Arguments = @(),
        [Parameter(Mandatory=$false)][string]$StdinText = "",
        [Parameter(Mandatory=$false)][string]$WorkingDirectory = $PWD.Path,
        [Parameter(Mandatory=$false)][hashtable]$EnvironmentVariables = @{},
        [Parameter(Mandatory=$false)][int]$TimeoutSeconds = 600,
        [Parameter(Mandatory=$false)][string]$RoleName = "CLI"
    )

    $pinfo = [System.Diagnostics.ProcessStartInfo]::new()
    $pinfo.WorkingDirectory = $WorkingDirectory
    $pinfo.UseShellExecute = $false
    $pinfo.RedirectStandardInput = $true
    $pinfo.RedirectStandardOutput = $true
    $pinfo.RedirectStandardError = $true
    $pinfo.StandardInputEncoding = [System.Text.UTF8Encoding]::new($false)
    $pinfo.StandardOutputEncoding = [System.Text.UTF8Encoding]::new($false)
    $pinfo.StandardErrorEncoding = [System.Text.UTF8Encoding]::new($false)
    $pinfo.CreateNoWindow = $true

    # 1. Windows Extension Routing
    $ext = [System.IO.Path]::GetExtension($ExecutablePath).ToLowerInvariant()
    if ($IsWindows -or $env:OS -eq "Windows_NT") {
        if ($ext -in @(".cmd", ".bat")) {
            $pinfo.FileName = "cmd.exe"
            $pinfo.ArgumentList.Add("/d")
            $pinfo.ArgumentList.Add("/c")
            $pinfo.ArgumentList.Add($ExecutablePath)
            foreach ($arg in $Arguments) { $pinfo.ArgumentList.Add($arg) }
        } elseif ($ext -eq ".ps1") {
            $pinfo.FileName = "pwsh.exe"
            $pinfo.ArgumentList.Add("-NoProfile")
            $pinfo.ArgumentList.Add("-File")
            $pinfo.ArgumentList.Add($ExecutablePath)
            foreach ($arg in $Arguments) { $pinfo.ArgumentList.Add($arg) }
        } else {
            $pinfo.FileName = $ExecutablePath
            foreach ($arg in $Arguments) { $pinfo.ArgumentList.Add($arg) }
        }
    } else {
        $pinfo.FileName = $ExecutablePath
        foreach ($arg in $Arguments) { $pinfo.ArgumentList.Add($arg) }
    }

    # 2. Environment Variables Injection
    foreach ($k in $EnvironmentVariables.Keys) {
        $pinfo.EnvironmentVariables[$k] = [string]$EnvironmentVariables[$k]
    }

    $process = [System.Diagnostics.Process]::new()
    $process.StartInfo = $pinfo

    $stdoutBuilder = [System.Text.StringBuilder]::new()
    $stderrBuilder = [System.Text.StringBuilder]::new()
    $syncLock = [object]::new()

    # 3. Thread-safe Event Handlers
    $process.add_OutputDataReceived({
        param($s, $e)
        if ($null -ne $e.Data) {
            [System.Threading.Monitor]::Enter($syncLock)
            try { [void]$stdoutBuilder.AppendLine($e.Data) } finally { [System.Threading.Monitor]::Exit($syncLock) }
            Write-Host $e.Data -ForegroundColor Gray
        }
    })
    $process.add_ErrorDataReceived({
        param($s, $e)
        if ($null -ne $e.Data) {
            [System.Threading.Monitor]::Enter($syncLock)
            try { [void]$stderrBuilder.AppendLine($e.Data) } finally { [System.Threading.Monitor]::Exit($syncLock) }
            Write-Host $e.Data -ForegroundColor DarkGray
        }
    })

    [void]$process.Start()
    $process.BeginOutputReadLine()
    $process.BeginErrorReadLine()

    # 4. UTF-8 Stdin Pipeline Input
    if (-not [string]::IsNullOrEmpty($StdinText)) {
        $process.StandardInput.Write($StdinText)
        $process.StandardInput.Flush()
    }
    $process.StandardInput.Close()

    # 5. Process Timeout Watchdog & Tree Kill
    $completed = $process.WaitForExit($TimeoutSeconds * 1000)
    if (-not $completed) {
        $pidToKill = $process.Id
        Write-Warning "[$RoleName] Execution exceeded timeout of $TimeoutSeconds seconds. Terminating process tree (PID $pidToKill)..."
        try {
            if ($IsWindows -or $env:OS -eq "Windows_NT") {
                & taskkill.exe /F /T /PID $pidToKill 2>&1 | Out-Null
            } else {
                $process.Kill($true)
            }
        } catch {}
        throw "EXECUTION_TIMEOUT: $RoleName CLI timed out after $TimeoutSeconds seconds."
    }

    $process.WaitForExit()
    $exitCode = $process.ExitCode
    $stdoutStr = $stdoutBuilder.ToString()
    $stderrStr = $stderrBuilder.ToString()

    return [ordered]@{
        ExitCode = $exitCode
        Stdout = $stdoutStr
        Stderr = $stderrStr
        Combined = ($stdoutStr + "`n" + $stderrStr).Trim()
    }
}
```

### 2.2 Provider 差异化调用规范 (Aider vs Copilot vs Claude vs Antigravity)

在 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1) 中规范各 Provider 的调用策略：

```powershell
function Invoke-DevTurn {
    param(
        [string]$Provider,
        [string]$Prompt,
        [int]$Round,
        [string]$SessionId,
        [string]$Model,
        [string]$ReasoningEffort,
        [scriptblock]$CustomHook,
        [string]$WorkspaceRoot
    )

    if ($null -ne $CustomHook) {
        & $CustomHook -Prompt $Prompt -Round $Round -Model $Model -ReasoningEffort $ReasoningEffort
        return
    }

    switch ($Provider.ToLowerInvariant()) {
        "aider" {
            # Aider requires --message-file to avoid CLI truncation
            $aiderCmd = Get-Command "aider", "aider.cmd", "aider.ps1", "aider.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
            if (-not $aiderCmd) { throw "PROVIDER_UNAVAILABLE: Aider CLI is not found in PATH." }
            
            $tmpPromptFile = [System.IO.Path]::GetTempFileName()
            try {
                [System.IO.File]::WriteAllText($tmpPromptFile, $Prompt, [System.Text.UTF8Encoding]::new($false))
                $argsList = @("--message-file", $tmpPromptFile, "--yes-always", "--no-auto-commits")
                if (-not [string]::IsNullOrWhiteSpace($Model)) { $argsList += @("--model", $Model) }

                $res = Invoke-CliWithTimeout -ExecutablePath $aiderCmd.Source -Arguments $argsList -WorkingDirectory $WorkspaceRoot -RoleName "Dev (Aider)"
                if ($res.ExitCode -ne 0) { throw "DEV_AGENT_EXECUTION_FAILED: Aider CLI exited with code $($res.ExitCode)." }
            } finally {
                if (Test-Path -LiteralPath $tmpPromptFile) { Remove-Item -LiteralPath $tmpPromptFile -Force -ErrorAction SilentlyContinue }
            }
        }
        "copilot" {
            $copilotCmd = Get-Command "copilot", "copilot.cmd", "copilot.ps1" -ErrorAction SilentlyContinue | Select-Object -First 1
            if (-not $copilotCmd) { throw "PROVIDER_UNAVAILABLE: GitHub Copilot CLI is not found in PATH." }
            
            $argsList = @("--allow-all")
            if (-not [string]::IsNullOrWhiteSpace($SessionId)) { $argsList += "--session-id=$SessionId" }
            if (-not [string]::IsNullOrWhiteSpace($Model)) { $argsList += @("--model", $Model) }
            $copilotEffort = Format-CopilotReasoningEffort $ReasoningEffort
            if (-not [string]::IsNullOrWhiteSpace($copilotEffort) -and $copilotEffort -ne "none") { $argsList += @("--reasoning-effort", $copilotEffort) }

            $res = Invoke-CliWithTimeout -ExecutablePath $copilotCmd.Source -Arguments $argsList -StdinText $Prompt -WorkingDirectory $WorkspaceRoot -RoleName "Dev (Copilot)"
            if ($res.ExitCode -ne 0) { throw "DEV_AGENT_EXECUTION_FAILED: Copilot CLI exited with code $($res.ExitCode)." }
        }
        "claude" {
            $claudeCmd = Get-Command "claude", "claude.cmd", "claude.ps1" -ErrorAction SilentlyContinue | Select-Object -First 1
            if (-not $claudeCmd) { throw "PROVIDER_UNAVAILABLE: Claude Code CLI is not found in PATH." }

            $argsList = @("--print")
            if (-not [string]::IsNullOrWhiteSpace($Model)) { $argsList += @("--model", $Model) }
            $envMap = @{}
            if (-not [string]::IsNullOrWhiteSpace($ReasoningEffort)) {
                $envMap["MAX_THINKING_TOKENS"] = switch ($ReasoningEffort.ToLowerInvariant()) {
                    "high" { "16384" }; "max" { "64000" }; "medium" { "8192" }; "low" { "2048" }; "off" { "0" }; default { $ReasoningEffort }
                }
            }

            $res = Invoke-CliWithTimeout -ExecutablePath $claudeCmd.Source -Arguments $argsList -StdinText $Prompt -WorkingDirectory $WorkspaceRoot -EnvironmentVariables $envMap -RoleName "Dev (Claude)"
            if ($res.ExitCode -ne 0) { throw "DEV_AGENT_EXECUTION_FAILED: Claude CLI exited with code $($res.ExitCode)." }
        }
        "antigravity" {
            $agyCmd = Get-Command "agy", "agy.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
            if (-not $agyCmd) { throw "PROVIDER_UNAVAILABLE: Antigravity CLI ('agy') is not found in PATH." }
            
            $argsList = @("--dangerously-skip-permissions")
            if (-not [string]::IsNullOrWhiteSpace($Model)) { $argsList += @("--model", $Model) }
            $agyEffort = Format-AgyReasoningEffort $ReasoningEffort
            if (-not [string]::IsNullOrWhiteSpace($agyEffort)) { $argsList += @("--effort", $agyEffort) }
            $argsList += @("--print")

            $res = Invoke-CliWithTimeout -ExecutablePath $agyCmd.Source -Arguments $argsList -StdinText $Prompt -WorkingDirectory $WorkspaceRoot -RoleName "Dev (Antigravity)"
            if ($res.ExitCode -ne 0) { throw "DEV_AGENT_EXECUTION_FAILED: Antigravity CLI exited with code $($res.ExitCode)." }
        }
        "mock" {
            Write-Host "[MOCK DEV] Simulating successful code modifications in $WorkspaceRoot." -ForegroundColor Gray
        }
        default {
            throw "Unsupported DevProvider '$Provider'."
        }
    }
}
```

### 2.3 Tracked & Untracked 安全 Diff 提取与 64,000 字符限制 (`Get-SafeWorkspaceDiff`)

```powershell
function Get-SafeWorkspaceDiff {
    param(
        [Parameter(Mandatory=$true)][string]$WorkspacePath,
        [Parameter(Mandatory=$false)][int]$MaxTotalChars = 64000,
        [Parameter(Mandatory=$false)][int]$MaxFileBytes = 262144
    )

    $diffOutput = [System.Text.StringBuilder]::new()
    $blacklistPatterns = @(
        '\.lock$', 'package-lock\.json$', 'pnpm-lock\.yaml$',
        '\.jar$', '\.exe$', '\.dll$', '\.png$', '\.jpg$', '\.jpeg$', '\.gif$', '\.ico$',
        'node_modules[\\/]', '\.git[\\/]', 'build[\\/]', 'target[\\/]', 'dist[\\/]',
        'review-mailbox\.json$', 'projects\.json$'
    )

    Push-Location $WorkspacePath
    try {
        # 1. Tracked Changes - File by File Stream
        $changedFiles = git diff --name-only -z HEAD 2>&1 | Out-String
        if ($LASTEXITCODE -eq 0 -and -not [string]::IsNullOrWhiteSpace($changedFiles)) {
            $filesList = $changedFiles -split "`0"
            foreach ($relFile in $filesList) {
                if ([string]::IsNullOrWhiteSpace($relFile)) { continue }
                $normalized = $relFile.Replace('\', '/')

                $isBlacklisted = $false
                foreach ($p in $blacklistPatterns) {
                    if ($normalized -match $p) { $isBlacklisted = $true; break }
                }
                if ($isBlacklisted) { continue }

                # Check if file size exceeds 256KB
                $fullPath = Join-Path $WorkspacePath $relFile
                if (Test-Path -LiteralPath $fullPath -PathType Leaf) {
                    $item = Get-Item -LiteralPath $fullPath
                    if ($item.Length -gt $MaxFileBytes) {
                        [void]$diffOutput.AppendLine("`n=== Tracked File: $relFile (Size: $($item.Length) bytes) - Exceeded 256KB limit, diff skipped ===")
                        continue
                    }
                }

                $fileDiff = git diff HEAD -- $relFile 2>&1 | Out-String
                if (-not [string]::IsNullOrWhiteSpace($fileDiff)) {
                    if ($fileDiff.Length -gt $MaxFileBytes) {
                        [void]$diffOutput.AppendLine("`n=== Tracked File: $relFile (Diff size: $($fileDiff.Length) chars) - Exceeded 256KB limit, diff skipped ===")
                    } else {
                        [void]$diffOutput.AppendLine($fileDiff)
                    }
                }

                if ($diffOutput.Length -ge $MaxTotalChars) { break }
            }
        }

        # 2. Untracked Files - File by File Stream
        if ($diffOutput.Length -lt $MaxTotalChars) {
            $untrackedStat = git status --porcelain -uall 2>&1 | Out-String -Stream
            foreach ($uLine in $untrackedStat) {
                if ($uLine.Trim() -match '^\?\?\s+(.*)$') {
                    $uPath = $Matches[1].Trim().Trim('"')
                    $normalizedU = $uPath.Replace('\', '/')

                    $isBlacklisted = $false
                    foreach ($p in $blacklistPatterns) {
                        if ($normalizedU -match $p) { $isBlacklisted = $true; break }
                    }
                    if ($isBlacklisted) { continue }

                    $fullUPath = Join-Path $WorkspacePath $uPath
                    if (Test-Path -LiteralPath $fullUPath -PathType Leaf) {
                        $item = Get-Item -LiteralPath $fullUPath
                        if ($item.Length -gt $MaxFileBytes) {
                            [void]$diffOutput.AppendLine("`n=== Untracked File: $uPath (Size: $($item.Length) bytes) - Exceeded 256KB limit, skipped ===")
                        } else {
                            try {
                                $content = [System.IO.File]::ReadAllText($fullUPath, [System.Text.Encoding]::UTF8)
                                [void]$diffOutput.AppendLine("`n=== Untracked File: $uPath ===`n$content")
                            } catch {}
                        }
                    }
                }
                if ($diffOutput.Length -ge $MaxTotalChars) { break }
            }
        }
    } finally {
        Pop-Location
    }

    $finalStr = $diffOutput.ToString()
    if ($finalStr.Length -gt $MaxTotalChars) {
        $finalStr = $finalStr.Substring(0, $MaxTotalChars) + "`n`n[WARNING: Git diff truncated at $MaxTotalChars characters to protect Reviewer context window. Remaining changes omitted.]"
    }

    return $finalStr
}
```

### 2.4 后端可测性导出、并发互斥与 `/api/stop` 讨论流中止

在 [`server.js`](file:///D:/project/dual-agent-studio/server.js) 中重构状态管控与生命周期：

```javascript
// Global Studio States
let activeProcess = null;             // Orchestrator child process
let activeDiscussionProcess = null;   // Active discussion agent child process
let activeConfig = null;
let isDiscussing = false;
let logs = [];
const sseClients = new Set();

// 1. /api/discuss - 202 Accepted & 409 Conflict Guard
if (pathname === '/api/discuss' && req.method === 'POST') {
    if (activeProcess || isDiscussing) {
        res.writeHead(409, { 'Content-Type': 'application/json' });
        res.end(JSON.stringify({ error: 'Another loop or discussion is currently in progress.' }));
        return;
    }
    
    let body = '';
    req.on('data', c => body += c);
    req.on('end', () => {
        try {
            const params = JSON.parse(body);
            if (!params.vaguePrompt) {
                res.writeHead(400, { 'Content-Type': 'application/json' });
                res.end(JSON.stringify({ error: 'vaguePrompt is required.' }));
                return;
            }

            // Immediately accept request
            isDiscussing = true;
            res.writeHead(202, { 'Content-Type': 'application/json' });
            res.end(JSON.stringify({ success: true, message: 'Discussion initiated in background.' }));

            // Run discussion workflow asynchronously with 600s watchdog
            runBackgroundDiscussion(params).finally(() => {
                isDiscussing = false;
                activeDiscussionProcess = null;
            });
        } catch (e) {
            res.writeHead(400, { 'Content-Type': 'application/json' });
            res.end(JSON.stringify({ error: 'Invalid JSON payload.' }));
        }
    });
    return;
}

// 2. /api/stop - Comprehensive abort for both loop & discussion
if (pathname === '/api/stop' && req.method === 'POST') {
    let stoppedSomething = false;

    // Stop active execution loop
    if (activeProcess) {
        appendLog('⚠️ 用户主动中止运行中的闭环任务...', 'system');
        try {
            if (process.platform === 'win32') {
                spawn('taskkill', ['/F', '/T', '/PID', String(activeProcess.pid)], { shell: true });
            } else {
                activeProcess.kill('SIGKILL');
            }
        } catch {}
        activeProcess = null;
        broadcast('state_change', { isRunning: false, stoppedByUser: true });
        stoppedSomething = true;
    }

    // Stop active discussion process
    if (activeDiscussionProcess || isDiscussing) {
        appendLog('⚠️ 用户主动中止运行中的需求推演与讨论...', 'system');
        if (activeDiscussionProcess) {
            try {
                if (process.platform === 'win32') {
                    spawn('taskkill', ['/F', '/T', '/PID', String(activeDiscussionProcess.pid)], { shell: true });
                } else {
                    activeDiscussionProcess.kill('SIGKILL');
                }
            } catch {}
            activeDiscussionProcess = null;
        }
        isDiscussing = false;
        broadcast('discussion_error', { error: 'Discussion stopped by user.' });
        stoppedSomething = true;
    }

    res.writeHead(200, { 'Content-Type': 'application/json' });
    res.end(JSON.stringify({
        success: true,
        message: stoppedSomething ? 'Process / Discussion stopped.' : 'No active process is currently running.'
    }));
    return;
}

// 3. Conditional listening for automated test support
if (require.main === module) {
    server.listen(PORT, '127.0.0.1', () => {
        console.log(`🚀 Dual-Agent Studio is running at: http://localhost:${PORT}`);
    });
}
module.exports = { server, getProjects, getModelsConfig };
```

---

## 3. 更新后的可执行任务分解清单 (Updated Actionable Subtask Checklist)

- [ ] **[Task 1] [P0] 前后端方法契约修复、DOM 控件补全与 Toast 系统落地**
  - 在 [`public/index.html`](file:///D:/project/dual-agent-studio/public/index.html) 中补充 `<input id="maxSelfHealAttempts" type="number" min="1" max="10" value="3">` 表单控件，添加全局 Toast 容器 `<div id="toastContainer"></div>`。
  - 在 [`public/app.js`](file:///D:/project/dual-agent-studio/public/app.js) 中统一实现并全局挂载 `openFolderPickerModal`, `closeFolderPickerModal`, `navigateUpFolder`, `confirmSelectedFolder`，并保留老方法别名兼容。
  - 在 [`public/app.js`](file:///D:/project/dual-agent-studio/public/app.js) 中实现轻量 Toast 通知函数 `showToast(msg, type)`，全面替换所有原生 `alert()` 调用。
  - 修复 `loadUserPreferences()` 与 `saveUserPreferences()`，消除 DOM 元素空指针异常。
  - 修复 [`server.js:detect-workspace`](file:///D:/project/dual-agent-studio/server.js) 响应体，同时返回 `verifyCommand` 与 `recommendedCommand` 双字段。

- [ ] **[Task 2] [P0] Orchestrator 核心函数库解耦与 Windows CLI 路由执行器重构**
  - 创建 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1)，抽离所有核心工具函数与执行函数。
  - 在 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1) 中实现 `Invoke-CliWithTimeout`：按扩展名（`.cmd`/`.bat` 路由至 `cmd.exe /d /c`，`.ps1` 路由至 `pwsh.exe -File`，`.exe` 直启）路由，建立 Stdin UTF-8 管道流，为 `OutputDataReceived`/`ErrorDataReceived` 的 `StringBuilder` 追加 `[System.Threading.Monitor]` 互斥锁，并在超时时执行 `taskkill.exe /F /T /PID` 强制清理子进程树。
  - 规范分 Provider 入参：Aider 采用 `--message-file` 临时文件传递长 Prompt，Copilot / Claude / Antigravity 走 Stdin 管道流。
  - 改造 [`engine/orchestrator.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator.ps1) 点源引入 `orchestrator-lib.ps1`，保留主执行逻辑。

- [ ] **[Task 3] [P0] Tracked & Untracked 逐文件流式 Diff 防护与通用自愈提炼**
  - 在 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1) 中实现 `Get-SafeWorkspaceDiff`：使用 `git diff --name-only -z HEAD` 逐文件获取 diff，结合黑名单过滤、单文件 256 KB 摘要替换以及全局 **64,000 字符** 预算截断。
  - 在 [`engine/orchestrator-lib.ps1`](file:///D:/project/dual-agent-studio/engine/orchestrator-lib.ps1) 中实现 `Extract-TestFailureSummary` 通用提取算法：基于失败特征关键词与其前后 3 行上下文提取错误摘要，硬上限限制为 8 KB。

- [ ] **[Task 4] [P0] 后端讨论流非阻塞 202 化改造、409 互斥锁与 `/api/stop` 全面中止**
  - 改造 [`server.js:/api/discuss`](file:///D:/project/dual-agent-studio/server.js)：接收请求后校验参数，立即返回 `202 Accepted`，并在后台启动异步推演；存在并发任务时直接返回 `409 Conflict`。
  - 在 `executeDiscussionAgent` 中将单轮超时上限设为 **600 秒**，并记录 `activeDiscussionProcess` 句柄。
  - 改造 [`server.js:/api/stop`](file:///D:/project/dual-agent-studio/server.js)：同时终止 `activeProcess` 与 `activeDiscussionProcess`，清理临时 Prompt 文件并重置 `isDiscussing` 标志。
  - 改造 [`server.js`](file:///D:/project/dual-agent-studio/server.js) 启动入口：满足 `if (require.main === module)` 时监听端口，导出 `server` 以支持单元测试。

- [ ] **[Task 5] [P1] 自动化验证套件构建与全链路门禁验证**
  - 在 [`tests/orchestrator.tests.ps1`](file:///D:/project/dual-agent-studio/tests/orchestrator.tests.ps1) 中：点源引用 `engine/orchestrator-lib.ps1`，编写针对 `Extract-TestFailureSummary`（8KB 截断）、`Get-SafeWorkspaceDiff`（单文件 256KB 跳过与全局 64,000 字符截断）、`Invoke-CliWithTimeout`（超时终止与 FAILED 状态回写）、Aider `--message-file` 参数构建的独立单元测试。
  - 新建 [`tests/server.tests.js`](file:///D:/project/dual-agent-studio/tests/server.tests.js)：通过动态端口 `PORT` 独立 `spawn` 服务进程，自动化测试 `/api/detect-workspace` 双字段、`/api/discuss` 202 异步响应与 409 冲突锁、`/api/stop` 中止状态清理。
  - 新建 [`tests/frontend-contract.tests.js`](file:///D:/project/dual-agent-studio/tests/frontend-contract.tests.js)：静态解析 `index.html` 与 `app.js`，严格断言 DOM ID 完整性与挂载函数契约。

- [ ] **[Task 6] [P2] 模型字典增量微调与最终验收**
  - 对 [`models-config.json`](file:///D:/project/dual-agent-studio/models-config.json) 进行增量微调（保持现有系列与模型结构完备性）。
  - 执行完整门禁命令验证套件，确保 100% 绿灯通过。

---

## 4. 自动化测试门禁验证命令与验收标准 (Verification Gates)

```powershell
# 1. 验证 Node.js 服务端语法与模块无缝加载
node --check server.js

# 2. 运行前后端 API 契约与接口自动化测试套件 (零外部依赖，独立端口隔离)
node tests/server.tests.js
node tests/frontend-contract.tests.js

# 3. 运行 Orchestrator 核心调度、超时守护、Diff 截断与状态机自动化测试套件
pwsh -NoProfile -File ./tests/orchestrator.tests.ps1

# 4. 校验 models-config.json 配置结构完整性
node -e "const cfg = JSON.parse(require('fs').readFileSync('./models-config.json', 'utf8')); console.log('✅ Models catalog valid, total series:', cfg.series.length);"
```

---

> **结语与落地准备**：本 Round 3 修订案已彻底闭合 Reviewer 提出的 3 项关键硬缺口，通过扩展名路由解决了 Windows 下 `.cmd` / `.ps1` 启动与 Aider 参数通道问题，重构了逐文件流式安全 Diff 并严格锁定了 64,000 字符上限，完成了 `orchestrator-lib.ps1` 模块解耦与讨论流 `/api/stop` 深度中止。所有方案均具备高度可测试性与工业级鲁棒性，已准备就绪进入编码实施。

---

### 📋 审查方确认之约束与测试门禁
Round 3 已覆盖先前 3 项，我再核对脚本作用域依赖、删除文件 diff，以及 Cursor/Codex 是否会回归。

Round 3 把架构缺口补上了，但示例代码仍有 4 处会让声称已闭合的 P0 失效。

## 样本缺陷（禁止原样落地）
1. **删除/改名大文件仍会撑爆内存**  
   工作树文件不存在时跳过了 256KB 检查，随后 `git diff HEAD -- file | Out-String` 会把整个 blob 读进内存。对缺失文件先用 `git cat-file -s HEAD:<path>`，超限只写摘要。`git diff --name-only -z | Out-String` 会在第一个 NUL 截断，改用换行列表。

2. **库抽取不能闭包脚本变量**  
   `Write-MailboxState` / `Read-MailboxState` 现依赖 `$effectiveMailboxPath`。抽到 `orchestrator-lib.ps1` 必须显式传 `-MailboxPath`。`Invoke-ReviewerTurn` 同样禁止 `-p`。

3. **中止不完整**  
   `/api/start` 在 `isDiscussing` 时必须 `409`。`runBackgroundDiscussion` 每轮开头检查中止标志，否则 `/api/stop` 杀完进程仍会开下一轮。

4. **禁止砍 Provider**  
   `cursor` / `codex` / `pi` 必须保留（stdin 或临时文件，禁止 `-p`）。`default { throw }` 是回归。

`npm test` 需串上 `node --check`、`server.tests.js`、`frontend-contract.tests.js`。

**[VERDICT: NEEDS_REFINEMENT]**（需进一步修改）

只补这 4 条实现约束后即可 Round 4 共识。不要再扩范围。